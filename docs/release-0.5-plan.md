# リリース v0.5.0 / v0.5.1 の計画（2026-09-20）

作成: 2026-09-20。現行の公開版は **v0.4.7**（main `b50a180`）。
作業は**ブランチ `v0.5.0`・別の作業ツリー**（`timecode-sync-player-v05`）で行う。main は公開版のまま置き、
**0.4.x に不具合が出たら hotfix ブランチを切って先に直す**（利用者の指示、2026-09-20）。

## 方針（利用者と合意、2026-09-20）

- **段階的に作り、v0.5.0 としてまとめて出す。** 整理だけで版を切らない（利用者から見て何も変わらないため）
- 中間は**候補ビルド**（版を上げず SHA で識別）で検証機へ送る。版を上げるのは全条件に通ってから
- **2 週間を超えそうなら切り替える。** 先に出せるものがあれば 0.5.0 をそちらで切り、HAP は 0.6.0 に回す
- 振る舞いに触る整理は、**その領域を別の理由で触るときに同乗**させる（検証 1 回を共有するため）

---

## v0.5.0: HAP の GPU 直接対応

**狙い**: VJ で使われる HAP を、CPU で展開して 33MB/コマ を転送する形ではなく、
**圧縮テクスチャのまま GPU へ渡して展開する**形で再生できるようにする。

### 根拠（測定済み）

[docs/analysis/2026-09-20-hap-gpu-probe.md](analysis/2026-09-20-hap-gpu-probe.md) の試作で、合格条件 3 つをすべて満たした。
- 4K60 HapQ（実写）で 1 コマ 中央 2.00ms / 最大 5.7ms（予算 中央 8ms・最大 12ms）
- 60 秒 × 48 条件で落ちコマ 0（1 条件だけ 7 件出たが再現せず）
- 読み出しは最悪ケース 475MB/s に対し 3,577MB/s
- qtdemux は 272〜363 コマ/秒（必要量の 4.5〜6 倍）

### 入れるもの

1. **shim に HAP の分岐**（`video/x-hap` の拒否をやめる）
   - いまは `on_video_pad` で明示的に拒否し、`avdec_hap`（CPU 展開）へ流れないようにしてある
   - 新しい経路: `demux → queue → appsink（video/x-hap）` で圧縮フレームを受け、
     **Snappy 展開 → BC テクスチャへ転送 → シェーダで BGRA へ展開 → 既存の `copy_ring_locked` でリングへ**
   - **リングとフェンスの仕組みには触らない**（壊れる範囲を shim の中に収める）
2. **HAP の解析と Snappy 展開**（`include/tcs_hap.h`、純粋なデータ処理）
   - かたまり分割（並列展開できる形式）にも対応。**実データで固定する**（ffmpeg の `-chunks` で作れる）
3. **対象は HapQ（Scaled YCoCg DXT5）と Hap1（DXT1）**
   - HapA（アルファだけの変種）/ Hap7 / HapH は今回の対象外。**Hap Q Alpha（HapM）は素材が出るまで未保証として置く**（利用者方針、2026-09-23。使う可能性があるので除外はしない。ffmpeg では書き出せないため、実際のソフトで書き出した素材で確かめてから対応する）。**読み込み時に「未対応」として扱い、黙って壊れないようにする**
4. **推奨外コーデックの警告（0.4.7）への追加**: HAP をどう分類するかを実測で決める
   （H.264＝推奨 / ProRes＝可 と同じ枠組み。**測る前に決めない**）
5. **既定は無効から始める**（環境変数で切替）。検証機で全条件に通ってから既定で有効にする
6. **文書**: 現場準備ガイド（HAP の扱い・必要な読み出し帯域）、設定リファレンス、リリースノート

### 同乗させる整理（振る舞いを変えない削除のみ）

外部レビュー（Codex、2026-09-20）の指摘のうち、**消すだけで振る舞いが変わらないもの**を入れる。
- **転送するだけのクラスの削除**: `ProjectSaveExecutor`（受け取った関数を呼ぶだけ）、
  `WindowLoadedUiInitializer`（4 つの Action を順に呼ぶだけ）。付随するテストも整理
- **古いキーフレーム警告の経路の削除**（`TCS_LONG_GOP_WARNING`）。
  判定方式が誤っていると自分でコメントに書いてあり、読み込み時のスキャン（0.4.5）に置き換わっている

**残すもの**: `TCS_SEEK_COST_HINT`。棚上げ中の「キーフレーム列を見て着地先を選ぶ」の比較対象で、
**どちらを採るか決まっていない**。決着した時点で、勝った方だけ残して消す。

### 進み方（開発機、2026-09-20）

| 段階 | 状態 | 根拠 |
| --- | --- | --- |
| 1. HAP の解析と Snappy 展開 | **済** | shim のテスト 24 件（実データ 4 通り。C# の実装と検査和が一致） |
| 2. 最小再生 | **済** | 1080p Hap1 / Hap Alpha / 4K HapQ が絵として出る（画面で確認） |
| 3. シーク・未対応変種 | **済** | シーク 3 回とも目標位置へ着地。Hap 7 / Hap HDR は読み込みを理由つきで失敗させる |
| 4. 性能（実機） | **済** | 4K60 HapQ を 20 秒: 2 秒あたり 120〜122 コマ（期待 120〜121）、落ちコマ 0、エラー 0 |
| 5. 既存の形式への影響 | **済** | 非E2E 2078 合格、全体 2171 合格 / 0 失敗 / 12 スキップ（E2E 83 合格 / 0 失敗）。`c51782b` |
| 6. 検証機での確認 | **済** | 候補 1〜5。途中で Single の端の扱いの不具合 3 件（以前から）と HAP の展開のロックを直した。候補 4（`5861a32`）で全条件合格、候補 5（`a820a5e`、HAP 既定有効）で最終確認。2026-09-24 に v0.5.0 として公開 |

**対応する変種**: Hap（DXT1）・Hap Alpha（DXT5）・Hap Q（Scaled YCoCg DXT5）。
**Hap 7（BC7）と Hap HDR（BC6H）は未対応**として読み込みを失敗させる。テクスチャとしては作れてしまうが、
色の戻し方が違うため**黙って違う絵を出すより断る**。素材が手に入って絵を確認できたら足す。

### 完了の条件（検証機）

1. いつもの一式（シナリオ 22 × 3 周 + L-1 × 6）が v0.4.7 と同等
2. **HAP の実素材が再生できる**（絵が正しい・音がある素材なら音も）。HapQ と Hap1 の両方
3. **4K60 の HAP で、試作と同じ合格条件**（1 コマの時間・落ちコマ 0）を実機で満たす
4. HAP でシーク・トラック切替・LTC 同期が、ほかの形式と同じように動く
5. **未対応の変種（HapM ほか）で、黙って壊れない**（警告を出して再生しない、など決めた動きになる）
6. 既存の形式（H.264・ProRes・VP9・AV1）の挙動が変わっていない

### 入れないもの（v0.5.0 の範囲外）

- **NotchLC**: GStreamer にも同梱 ffmpeg にもデコーダが無い。対応するなら自前実装からで、素材を作るにも Notch 本体が要る。**別途、実現性の調査から**
- HAP の書き出し（エンコード）
- 圧縮テクスチャのままリングに流す経路（試作で差が 0.05ms 以内だったため、**約束を変えない方を採る**）

---

## v0.5.1: 整理と地固め

**2026-09-24 公開（候補 5 `f238f32` で合否の一式に合格）。** 項目 1・2 は入れた。項目 3 は化けた LTC の Jump の確認と、プレビューの停止の記録を入れた。項目 4 は見送り（下記）。検証中に見つかった v0.5.0 の Single の不具合（S-4）も直した。

**狙い**: 0.5.0 で先送りにした「振る舞いに触る整理」を、**検証 1 回でまとめて**片付ける。

### 入れるもの

1. **同期の取得結果の一体化**（Codex 指摘 2）
   - `ReadSyncTimePos` が取得結果をフィールドに保存し、`ReadSyncPositionSample` が取り出して消す。
     **「この順で呼ぶ」という暗黙の約束**が 2 つの関数に分かれている
   - 同じ取得で得た再生秒数と位置サンプルを**1 つの戻り値**にする
   - **同乗先があればそちらへ前倒しする**: 交番の修正（`steady-delivered-basis`）、
     自動化用に「LTC と位置を 1 回で読む口」を足す作業（L-1 の M4 の根治）。どれも同じ場所を触る
2. **ギャップのフリーズ確定処理の整理**（Codex 指摘 3）
   - `TryCaptureGapFreezeFrameAsync` は実際には待たず、条件判定を `Task.FromResult` で返すだけ。
     画像の保存は GPU 側（`ComposeLayer.SaveFreeze`）が担当しており、名前と構造が役割に合っていない
   - 同期的な状態遷移として整理する。**古い世代・古い捕捉要求を拒否する判定と、GPU 側の画像保持は残す**
3. **長時間試験で出た課題**（2026-09-20 に検証機で実施中の Single 4 時間・Continue 周回・切替集中）
   - 出た問題の大きさによっては、hotfix（0.4.x）へ回す

4. **トラック切替の直後に、読み込み所要ぶん手前から始まる件**（2026-09-20 の検証機の測定で確定）
   - Continue でトラックを切り替えると、**新しいトラックは頭（MediaIn 直後）から始まり**、その時点の
     タイムコードが指す位置より **0.05〜1.08 秒手前**になる。アプリはそれをシーク（または速度補正）で詰める
   - **遅れの量は「切替を決めてから絵が出るまでの所要」と 1 対 1 で一致**する。素材が重いほど大きい
     （実測: 推奨素材 0.05〜0.24 秒、長 GOP の VP9 4K60 で 1.08 秒）
   - **v0.4.6 と v0.4.7 で同じ**（中央 0.713 / 0.715 秒）。以前からの性質で、0.4.7 の回帰ではない
   - 直し方の候補は「読み込みにかかる時間を見越して先を狙う」だが、**0.4.5 の D37-h（見積もりで先を狙って
     行き過ぎ、既存の合格を 5 件壊した）と同じ形**になりうる。**見積もりの作り方を変える**必要がある
     （素材ごとの実測ロード時間を学習する、など）。同期の中心を触るので、**検証は一式やり直し**
   - **結論（2026-09-24、利用者と決定）: 見送り。** 検証機の RTX で、推奨素材（H.264）の切替直後の遅れは
     0.05〜0.07 秒で、既存の追い付きで 0.15 秒ほどで収まる。遅れが大きいのは ProRes（CPU 復号、
     ハードウェアデコーダーを開発中）と長 GOP の素材（書き出し直すべき素材）だけで、どちらも対象外とした。
     トラックごとに所要を学習して先から読み込む方式を候補 1・3 まで作ったが、同期の中心に学習状態が増え、
     候補 1 は軽い素材で先回りし過ぎた。挙動の安定を優先して外した（経緯は
     [analysis/2026-09-23-switch-load-lead.md](analysis/2026-09-23-switch-load-lead.md)）

### 完了の条件

1. いつもの一式が v0.5.0 と同等
2. **ギャップの出入り・トラック切替が、整理の前後で同じ挙動**（Continue の周回ソークで確認）
3. 同期の誤差の分布が v0.5.0 と同等（L-1、`sync.evaluate` の delta で判定）

---

## v0.5.2: 同期の状態の作り直し（2026-09-24、利用者と決定）

**狙い**: 不具合ごとに足してきた同期のラッチ（`_jumpAppliedOnce`・`_heldReapplyDone`・`_pendingJumpSeconds`・
`_heldLossLandingSeconds`・`_fileLoadReleasePending` など）を、状態と遷移が見える形に作り直す。
D33×D35 で S-3 が回帰し、v0.5.0 の S-4（端へのシーク記録の持ち越し）もラッチの寿命が原因だった。

1. **設計書を先に書き、承認を得てから実装する**: 状態（追従 / 保持停止 / 保持ランスルー / 境界ホールド /
   ロード中 / ギャップ）、遷移、各ラッチの行き先の表。振る舞いは変えない（既存の単体と E2E が判定）
2. **開発機の「重い素材セット」**: 推奨素材（H.264 High）の重い条件だけ。4K60、音声付き（48kHz / 44.1kHz）、
   解像度・fps・音声構成が切り替わる並び、音声の第 1 ch に LTC を含む素材。ProRes・長 GOP・AV1 は作らない。
   候補を検証機へ送る前に、同じ 3 本立てを開発機で通す
3. 検証は v0.5.1 と同じ固定の一式（標準シナリオ 3 通り＋L-1 6 本＋A 切替、RTX）

## 参考: 棚上げ中のもの（この 2 版には入れない）

見直しの条件つきで置いてある。
- **交番（位置の問い合わせが 2 値を返す）**: 修正は `steady-delivered-basis` に実装済み・既定 off。**効果は検証機でしか測れない**
- **復号律速の行き過ぎ**: 0.4.7 で表示だけ入れた。**現場で実際に出るかを見てから**、制御を変えるか決める
- **キーフレーム列を見た着地先の選択**: `keyframe-seek-target` に純関数あり。目的関数の作り直しが要る
- **自動化用に LTC と位置を 1 回で読む口**: L-1 の M4（UI の読みが遅い素材）の根治

## v0.5.2 最終候補（2026-09-25 22:20、検証機へ送付）

- ProductVersion `0.5.1+f5864c5299fc8c6be610025fc6c43c9ed28b0a32`（版は据え置き）。コードは `47d624f` と同じ（以降は文書だけ）。shim は v0.5.1 と同じ Release ビルド
- zip `CEE53F4FAEC3152052686F99182E239FC1B919585F2356DC361EDBED4700B881` / setup.exe `1640F6FD1C6E524345DAECAD6C626A3F2DD8735D913D8B6B63F640747C4D44EE`
- テストのソース `tcs-v052-f5864c5.tar`（`99E1A7EC…6C17`）。Taildrop で送付、固定の一式 2 回を依頼
- 公開済みの v0.5.1 の成果物は `artifacts/release/*.released-767e326` に改名して退避（同じ版番号で上書きされるため）

## 既知の間欠（v0.5.3 か v0.6.0 で扱う候補）

- **A1 型の最終フレームの取り込みの時間切れ（ギャップの Freeze）**: 最終フレーム（尺の直前）へのシークが EOS と重なると、
  3 秒の時間切れまでフレームが届かず、位置は EOS 側・絵は直前のまま残る。D32 の遅延確定の後も出る。
  開発機の標準素材で F-4 が 2 / 15（段 2 完了後）・0 / 17（段 1 直後）、取り込み 2 / 151・0 / 156（2026-09-25、設計書 v0.5.2 §11-3）。
  解析は `docs/analysis/2026-09-17-A1-gap-freeze-frame-not-updated.md`。製品側の未解決項目

## v0.5.2 の公開（2026-09-26 05:55）

- タグ `v0.5.2` = main `b9dc877`、ProductVersion `0.5.2+b9dc877…`。shim は v0.5.1 と同じ Release ビルド（`5DAF3C8D…`）
- zip `0B27D87117EF0202A17504260649D6F3FCA6BD42A46D99CD17CDA7B5CB6D15DE` / setup.exe `67B633002C8CC1226D2246A70B9C77FD7D6C9C8C4B7CE93D9D535D1379B59D98`（アップロード後の digest も一致）
- 検証機が試験した候補は `0.5.1+f5864c5`（コードは同じ。以降は文書と版の番号だけ）
- 罠（今回見つけた）: main の作業ツリーの `native/gst-shim/build-release/tcs_gstreamer.dll` は 09-19 の古いビルド（`E28F6E75…`）だった。検証済みの `5DAF3C8D…` を置いてからパッケージした（古いほうは退避）

## D38: ランスルーで保持後の同じトラック内のジャンプの着地が約 2 秒遅れる（2026-09-26 起票、v0.5.3 で直す）

- 発見: TSP-Fable（利用者の質問「C1 の失敗は復号速度か」の解析）。親がアプリのログで確かめた（2026-09-25 14:37:21〜24）
- 事実: 開発機の hold-landing の所要が、C-1（ランスルー、同じトラック内の往復）で 09-17 は 0.25〜0.61 秒、09-20 以降は最初の 1 回を除き 20 回中 19 回が 2.0〜2.2 秒。
  S-2（停止モード）・C-2（別トラック）は変わらない。4K でも 720p でも同じ
- 機序（推定）: 保持中の着地シークの保留が Settled にならず、次の Jump の同期要求が保留の時間切れ（約 2 秒）まで Deferred になる。
  距離 4×tolerance を超える置き換えの規則が効いていない。時間切れの後にシークのゲートでさらに 0.2〜0.4 秒
- 影響: ライブでタイムコードを別の位置から再開したときに約 2 秒遅れる。v0.4.x 以降と v0.5.2 でも起きる
- 次: w5:p3 に二分探索（09-17 postmerge3 相当〜09-20）・原因の特定・直し方の案。実装は親が設計を見てから。E2E の着地の判定に「4×tolerance 超のジャンプは 1.0 秒以内」を足す
- 既知の間欠の記録の訂正: 開発機の重い素材での C1 の失敗は D38 の現れ（設計書 v0.5.2 §12 の訂正）

## v0.5.4: シーク経路の門の統合（2026-09-26、利用者の決定。TSP-Fable 経由）

- 版の並び: v0.5.3（段 3 + D38、現在）→ **v0.5.4（門の統合）** → v0.6.0（ProRes の GPU 復号 + PowerShell 7）。v0.6.0 の中身と決定事項は変えない
- 着手は v0.5.3 の公開後。対象は [design/v0.5.3-d38-seek-gates.md](design/v0.5.3-d38-seek-gates.md) §3 の 24 門。§4 の統合案と §8 の改善候補を含める
- 目標: 着地の判定は 1 つ、Jump の雑音除去は 1 つ、シーク可否の門は 1 つ、一時停止の持ち主は 1 つの集合
- 進め方: 段 0（門ごとの発火回数・遅延を固定の一式で測って基準）→ 設計書（門ごとに残す／畳む／消すと根拠、利用者に諮る点）→ 親の承認 → 1 門 1 コミット、赤いテストから
- 併走: in-process の決定的なシナリオ層（`SyncScenarioHarness` の拡張、仮想時計＋偽の再生 API）の設計書を p6 に書かせる
- 合格の条件: 固定の一式 2 回 + 重い素材 3 本立て + 検証機。平常時の遅延の前後比較（着地時間の中央値、pending の時間切れ、シーク本数、jump-black、hold-pause 超過）で悪化ゼロ、門の数と遅延の減少
