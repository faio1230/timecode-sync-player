# v0.5.4: シーク経路の門の統合（設計書、親の案）

作成: 2026-09-26、TSP-Opus（親）。対象: `docs/design/v0.5.3-d38-seek-gates.md` §3 の 24 門。
基準: `docs/design/v0.5.4-gate-baseline.md`（段 0。標準の素材 2 周・重い素材 1 周の発火回数と遅延）。
位置づけ: `docs/release-0.5-plan.md` の「潰す一覧」K8。v0.5.4 は安定版（`docs/GOAL-v0.6.md` 0 節）。

## 0. 目標と合否

- 目標（利用者の決定 2026-09-26）: 「着地の判定は 1 つ、Jump の雑音の除去は 1 つ、シークしてよいかの門は 1 つ、一時停止の持ち主は 1 つの集合」
- 合否（GOAL 1 節）: 固定の一式 2 回＋重い素材 3 本立て＋検証機。加えて**平常時の遅延の前後比較**で悪化 0、門の数と遅延の減少を示す。
  比べる指標は段 0 と同じ（`scripts/analyze-v054-gates.py` の出力: 門ごとの発火回数と遅延、着地の遅れ、pending の時間切れ、シーク本数、通り過ぎの戻し、同じ目標への 3 本目以降、黒フレーム、一時停止の遅れ）
- やり方: 1 門（または 1 つの畳み）1 コミット。守っていた欠陥の再発を、決定的なシナリオ層（`docs/design/v0.5.4-scenario-layer.md`）の単体で先に押さえてから触る

## 1. 4 つの質問に門を割り当てる

| 質問 | 門（§3 の番号） | 段 0 の平常時の発火（標準 2 周） |
| --- | --- | --- |
| A. 着地したか | 6 着地判定、7 保留の時間切れ、9 着地後の抑止、10 シーク中の位置の不信、11 時間切れ後の再確認 | 6=140、9=24、10=152、7=0、11=0 |
| B. この Jump は本物か（雑音の除去） | 1 損失の確定 250ms、2 保持直後の Jump の即時復帰、3 JumpAppliedOnce のラッチ、4 Jump の確認窓 | 1=244、2=214、4=152、3=0 |
| C. いまシークしてよいか | 5 保留の抑止、8 置き換え、12 未信頼の決定、13 D37-a のゲート、14 デバウンス 250ms、15 速度補正優先、16 着地窓、22 手動・ネイティブのシーク中、24 連鎖の歯止め | 5=848、12=322、14=287、13=118、16=147、22=13、8=0、15=0、24=0 |
| D. 誰が止めているか（一時停止の持ち主） | 20 境界ホールド中の保持着地のスキップ、ほかに信号断・ギャップ・プロジェクト復元（v0.5.2 の持ち主の集合） | 20=0 |
| その他（残す） | 17・18 ロード、19 保持着地の 1 フレーム省略、21 保持値の変化の 1 回着地、23 補正の着地窓 | 18=150、23=8、17・19・21=0 |

## 2. 門ごとの案

| 門 | 案 | 畳み先・根拠 | 再発を押さえるテスト（シナリオ層） |
| --- | --- | --- | --- |
| 6 | **残す（A の唯一の判定）** | 着地の判定をここ 1 つにする。D38 (a) で保持中も判定するようになった | 既存の D38 の単体 |
| 10 | **畳む → A** | 「シーク中は位置を信用しない」は、A が pending を持っている間という状態そのもの。別の信頼フラグを持たず、A の状態から導く | シーク発行から着地までの位置で判定しない（D37-b） |
| 7・11 | **畳む → A（時間切れの枝）** | 平常時 0。時間切れと、その後の位置の安定の再確認を、A の「着地できなかった」枝として 1 か所に置く | 着地しないシーク（偽の再生 API で着地を止める）→ 2 秒で解除 → 安定 3 サンプルで再開 |
| 9 | **畳む → A** | 「直前に同じ目標へ着地した」は A の記録。別の 500ms 窓を持たない | 着地直後の同じ目標への再シークが出ない（D7-a） |
| 1・2 | **残す（B）** | 250ms の確定は無音と保持を分けるのに要る（D27）。2 は区別が付いた場合の即時の近道 | 既存の D27 系 |
| 3 | **消す** | 平常時 0（D38 の後）。捨てるときはログなしで Jump を落としていた。B の確認窓（4）と A の状態で足りる | 保持の後に 250ms を過ぎて届いた Jump が捨てられずに適用される |
| 4 | **残す（B の唯一の確認）** | 写像がギャップ／別トラックの Jump の確認（D30/D31） | 既存の D30/D31 系 |
| 12・5 | **畳む → C（A の状態で止める）** | 5（pending がある間は出さない）と 12（未信頼の間は出さない）は、どちらも A が着地を待っている間という 1 つの条件になる | 着地を待つ間に新しいシークが出ない。遠い要求は D38 (b) のとおり pending を捨てる |
| 8 | **残す（C の中、D38 (b)）** | 平常時 0 だが、到達不能な pending を 2 秒待たずに捨てる（D38 の修正そのもの） | 既存の D38 の単体 |
| 13 | **残す（C の唯一の雑音の門）** | 3 サンプルの中央値でシークを決める（D37-a） | 既存の D37-a 系 |
| 14 | **消す（候補、§3 の測定で決める）** | 13 のゲートが 0.25 秒窓で 3 サンプルを要るので、250ms のデバウンスと同じ質問に答えている。平常時に全シナリオで発火し、連続区間で中央 234ms を足している | 連続したシークの要求が 13 だけで 250ms 以内に 2 本出ない |
| 15・24 | **残す（C の中、平常時 0）** | 速度補正の優先（D37-b）と Jump の連鎖の歯止めは、平常時に遅延を足していない。消すと守っていた型（連鎖）が戻る恐れがある | 既存 |
| 16 | **残す（C の中）** | 着地窓は遅延を減らす側の門 | 既存の D37-b2/d/e |
| 22 | **残す（C の中）** | 手動操作の間は同期のシークを出さない | 既存 |
| 20 | **畳む → D** | 境界ホールド中の保持着地のスキップを、持ち主の集合の判定で置き換える（境界ホールドが持ち主なら着地しない） | 境界ホールド中は保持着地を出さない（D35-b） |
| 17 | **消す（到達しない分岐）** | 段 0 で 0 件。呼び出し側の `TryMarkFileLoaded` が先に止めるので、`ShouldSuppressSeek` のロード中の分岐は通らない。5 秒の安全タイムアウトは 18 側に 1 つだけ残す | ロード中にシークが出ない（18 の既存テスト） |
| 18・19・21・23 | **残す** | 18 はロードの成立。19・21 は保持の着地の重複を消す門で遅延を足さない。23 は補正の質問（シークの可否ではない） | 既存 |

門の数: 24 → **15**（消す 3: 3・14・17。A の判定に畳む 4: 7・9・10・11。5 と 12 を 1 つの条件に: 1 減。20 を D の集合に: 1 減）。
遅延の減少の見込み: 14 を消すと、連続区間の中央 234ms がなくなる。10・12・5 の一本化は、着地の判定と同じ時点で解けるので遅延は増えない。

## 3. 測定で決めること

- 14（デバウンス）を消した後、13 だけで連続シークが増えないか。段 0 の「同じ目標への 3 本目以降」（発振）が 0 のままであること、同期シークの本数が増えないことを、標準 2 周と重い素材で確かめる。増えたら 14 は残し、表に理由を書く
- 10・12・5 の一本化の前後で、S-2・C-1 の着地の遅れと、未信頼の間に出たシーク（0 であること）を比べる

## 4. §8 の改善候補（停止モードの Jump 直後の即時停止）

- D38 文書 §8: 停止モードで Jump の直後に Duplicate が続いたら、250ms を待たずに一時停止する。戻しのシークが 1 フレーム以内に収まり、門 19 で消える
- これは**振る舞いの変更**（停止が最大 250ms 早くなる）なので、GOAL 3 節の「挙動を変える判断」に当たる。**利用者に諮る**（下の 5 節）

## 5. 利用者に諮ること

1. §8 の改善を入れてよいか（停止モードで、LTC が飛んでから止まったときに、映像がすぐ止まる。いまは約 0.25 秒進んでから戻る）
2. 14（デバウンス）を消したときに、LTC の信号が乱れた現場で同期のシークの回数が増える危険。測定で増えなければ消す、増えたら残す、という決め方でよいか

### 決定（2026-09-26、利用者の回答）

1. **§8 の改善を入れる**（停止モードで、Jump の直後に Duplicate が続いたら待たずに一時停止する）
2. **14（デバウンス）は測定で決める**（消して、標準 2 周・重い素材でシークの本数と発振が増えなければそのまま、増えたら残す）

## 6. 段取り

1. シナリオ層の C4（門の観測、`sync.gate` の Debug 行を仮想時刻の sink で拾う）を入れる
2. 各案の「再発を押さえるテスト」を、今のコードで緑のまま先に入れる（守っている型を固定する）
3. 1 コミットずつ畳む／消す。各コミットで非E2E 全件、区切りごとに LTC シナリオ 1 周で段 0 の指標を比べる
4. 全部入ったら、標準 2 周・重い素材 3 本・検証機の固定の一式 2 回で前後比較の表を出す
