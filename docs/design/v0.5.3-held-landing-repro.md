# v0.5.3 段 3j: §6 の 4 の再現（保持損失 → Jump 復帰 → 無音の再損失）

- 手順: 停止モードで保持値 20.0 の損失（一時停止・20.0 へ着地）→ 値が動いた Jump 23.0 の 1 枚で即時復帰（D27-b）→ Normal を入れず再生を数フレーム進めて無音 → 2 度目の損失。
- 結果: **再現した。** 2 度目の無音損失で Jump 前の古い保持値 20.0 へのシークが出る（`HeldLossLanding` / `LastHeldEffective` が Jump 復帰で下りず、`ReapplyHeldValueOnPause` が無音なのに古い保持値へ着地する）。Normal を 1 枚でも入れると `OnNormalFrame` が 2 つを下ろすため起きない。
- 症状（利用者から見た）: Jump で新位置へ復帰した直後に LTC が無音になると、映像が Jump 前の停止位置へ跳んで戻り、新しい位置へ同期し直すまで古い位置に留まる。
- テスト: `HeldLandingAfterJumpReproTests`（Normal なし = Skip 付き。Skip を外すと赤 `found {20.0}`。Normal あり = 緑）。
