# v0.3 Review Fixes Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Fix the four v0.3 review findings, lock the LTC device-enumeration error display against tick refresh, and regenerate verified 0.3.0 release artifacts.

**Architecture:** Keep the existing X1 display pipeline: `_lastLtcFormatText` is the durable normal-status source and `RefreshLtcDisplayState()` is the only projection to the ViewModel. Preserve signal-loss and pause ownership behavior; only correct display wiring, UI Automation metadata, comments, and redundant tests.

**Tech Stack:** .NET 8, WPF, xUnit, FluentAssertions, FlaUI UIA3, PowerShell, Inno Setup 6.

## Global Constraints

- Work on `feature/v0.3-ux`; do not push, merge, or tag.
- Do not stage the untracked root `AGENTS.md`.
- Commit messages must be Japanese.
- Run non-E2E and hardware-inclusive E2E suites with zero failures and zero skips.
- Regenerate Release zip and setup.exe and record new SHA-256 values.

---

### Task 1: Persist the device-enumeration error

**Files:**
- Modify: `src/TimecodeSyncPlayer/MainWindow.xaml.cs`
- Create: `tests/TimecodeSyncPlayer.Tests/MainWindowLtcDisplayWiringTests.cs`

**Interfaces:**
- Consumes: `_lastLtcFormatText`, `RefreshLtcDisplayState()`.
- Produces: durable `LTC デバイス列挙失敗` display state across repeated tick refreshes.

- [ ] **Step 1: Write the failing wiring regression test**

Read `MainWindow.xaml.cs`, isolate the `RefreshLtcDevices` catch block, and assert it assigns `_lastLtcFormatText = "LTC デバイス列挙失敗";` before calling `RefreshLtcDisplayState();`, with no direct `_vm.Sync.LtcFormatText` assignment.

- [ ] **Step 2: Run the test and verify RED**

Run: `dotnet test tests\TimecodeSyncPlayer.Tests\TimecodeSyncPlayer.Tests.csproj --filter "FullyQualifiedName~MainWindowLtcDisplayWiringTests"`

Expected: FAIL because the current catch block writes directly to the ViewModel.

- [ ] **Step 3: Apply the minimal production fix**

Replace the direct assignment with:

```csharp
_lastLtcFormatText = "LTC デバイス列挙失敗";
RefreshLtcDisplayState();
```

- [ ] **Step 4: Run the regression test and verify GREEN**

Run the Task 1 filter again and expect 1/1 pass.

### Task 2: Apply the three minor review fixes

**Files:**
- Modify: `src/TimecodeSyncPlayer/MainWindow.xaml`
- Modify: `src/TimecodeSyncPlayer/MainWindow.xaml.cs`
- Modify: `tests/TimecodeSyncPlayer.Tests/E2E/LtcHardwareLoopE2ETests.cs`
- Modify: `tests/TimecodeSyncPlayer.Tests/LtcDisplayStateFormatterTests.cs`

**Interfaces:**
- Consumes: `Sync.LtcTimecodeForeground`, FlaUI automation properties.
- Produces: color status via `AutomationProperties.ItemStatus`, an ordering invariant comment, and removal of the redundant stateless formatter test.

- [ ] **Step 1: Change accessibility metadata and E2E reader**

Bind `AutomationProperties.ItemStatus` instead of `HelpText`, rename `ReadHelpText` to `ReadItemStatus`, and read `element.Properties.ItemStatus.ValueOrDefault`.

- [ ] **Step 2: Document the stopped-event ordering invariant**

Immediately before `IsLtcRunning = false`, explain that `MarkStopped` must run first so the PropertyChanged handler does not replace the chosen stopped/error format text.

- [ ] **Step 3: Remove the redundant recovery formatter test**

Delete `Format_WhenSignalRecovers_RestoresCurrentFormatAndActiveColor`; the formatter is stateless and the receiving-state test already covers the same branch.

- [ ] **Step 4: Run focused tests**

Run the wiring, formatter, and hardware signal-return filters; expect all to pass.

### Task 3: Mutation, full gates, release packaging, and commit

**Files:**
- Modify: `docs/test-coverage-progress.md`

**Interfaces:**
- Consumes: review-fix implementation and existing package script.
- Produces: verified commits, regenerated artifacts, hashes, and final goal evidence.

- [ ] **Step 1: Confirm mutation detection**

Temporarily restore the direct ViewModel assignment/remove the refresh call, run `MainWindowLtcDisplayWiringTests`, verify failure, restore the fix, and verify pass.

- [ ] **Step 2: Run full Debug gates**

Run non-E2E and all hardware-inclusive E2E tests with `$env:windir=$env:SystemRoot`; expect zero failures/skips/warnings.

- [ ] **Step 3: Commit the review fixes**

Stage only the plan, production, and test files and commit with a Japanese message.

- [ ] **Step 4: Run Release gates and regenerate artifacts**

Run Release non-E2E, Release E2E with `TIMECODE_SYNC_PLAYER_E2E_APP_PATH` fixed to the Release executable, then `scripts/package-release.ps1`.

- [ ] **Step 5: Verify and record artifacts**

Verify zip required entries and exclusions, setup/app version 0.3.0, compute SHA-256 and sizes, append results to `docs/test-coverage-progress.md`, and commit the record in Japanese.

- [ ] **Step 6: Final audit**

Confirm branch, clean tracked state, untracked `AGENTS.md` only, zero residual app processes, unchanged hashes, and no push/tag/merge.
