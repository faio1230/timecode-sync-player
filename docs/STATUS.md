# 現在地（2026-09-25）

- **公開**: v0.5.1 (beta)、main = タグ `v0.5.1` = `767e326`
- **作業中**: v0.5.2（ブランチ `v0.5.2`、作業ツリー `timecode-sync-player-v05`）。同期の状態を見える形にする作り直し
  - 段 0 完了: ラッチの寿命の特性テスト 425 行（`tests/TimecodeSyncPlayer.Tests/LatchLifetime/`）、全件緑、表は承認済み
  - v0.5.2 の中の順番: 段 1 → 段 2（軸ごとの状態の型と、軸をまたぐ規則の純関数）＋ §6-11・§6-12 →
    候補を作り、開発機で重い素材セット → 検証機で固定の一式を 2 回
- **ゴール**: [GOAL-v0.6.md](GOAL-v0.6.md)（v0.5.2 → v0.5.3 → v0.6.0 を自律で進める。到達条件・版ごとのブランチの流れ・止まる地点）
- **次の 3 手**:
  1. v0.5.2: **合格（2026-09-26 02:25、検証機の固定の一式 2 回で v0.5.1 から落ちた項目なし）。版を 0.5.2 に上げた（`eed90ae`）**
     - U-1 は v0.5.1 でも同じ型（交互 3 回ずつで 6/6）、C-1 の 1 回は試験側の LTC の途切れ。L-1 12 本・A 2 本は全合格
     - **止まっている（利用者に諮る）**: (1) main の作業ツリーに利用者の未コミットの変更（README・ARCHITECTURE・ROADMAP・UI-UX-DESIGN・図、
       2 つの LTC スクリプトの -GapSeconds）があり、main へ統合できない。スクリプトの -GapSeconds は v0.5.2 側にもある。
       (2) 統合・タグの後の公開の許可
  2. v0.5.3 = 段 3（寿命の食い違いを赤いテストから 1 件ずつ直す。§6 の 2 → 1、3、5、10、8、9、13、14）。`main` から `v0.5.3` を切る（GOAL 2 節）
     - 担当ブランチ `agent-a-v053-3a` の進み: 3a 経路の棚卸し `97e0c19` → 3b GPU 復旧・自動送りを BeginFileLoad に通す `05b56f4` →
       3c 境界ホールドのラッチをモード切替・同期の無効化・停止で消す `e305ba6`（ラッチだけ消し一時停止は変えない。読み込みの行は直さない）→
       3d ロード中の印を同期の無効化・停止で取り消す（§6 の 3、w5:p3 作業中）。残り: §6 の 5、10、8、9、13、14
     - **利用者に諮ること（範囲の変更）**: §6 の 2 は「読み込み 4 経路」で承認されたが、#3 ギャップの LoadPausedAt と #4 Freeze の読み直しは
       そのまま BeginFileLoad に通すと Freeze の取り込みが壊れる（一時停止のままロード解除が最大 5 秒遅れ、全シークが止まる）。
       (a) ロード中の印を立てずに保留・位置の信頼・読み込み番号だけを切る別の口を作って v0.5.3 で直す、(b) v0.5.3 では見送り既知として記録、のどちらか
  3. v0.6.0 = ProRes の GPU 復号（[release-0.6-plan.md](release-0.6-plan.md)。プラグイン側の前準備は受け取り済み、v0.5.3 の後に設計）。
     決定済み: GPU 復号は NVIDIA だけ既定で有効（他は prores-cpu、フォールバックは全ベンダー）、設定キー `proResGpu`（auto / on / off）と UI の 3 択
- **決まったこと（2026-09-25、利用者）**: できごとの列は開発機で段 1 直後（`a59a53f`）と段 2 完了後の候補を突き合わせるだけ（検証機は最終候補の固定の一式 2 回だけ）。
  §6 の 13・14 は v0.5.3 で直す（順番は 2 → 1、3、5、10、8、9、13、14）。設計書 §5・§7

## 規則と入口

- 設計: [design/v0.5.2-sync-state.md](design/v0.5.2-sync-state.md)（§5 版の分け方、§7 決定事項、§8 レビュー）
- 計画: [release-0.5-plan.md](release-0.5-plan.md)、[release-0.6-plan.md](release-0.6-plan.md)、ゴール [GOAL-v0.6.md](GOAL-v0.6.md)
- 引き継ぎ: [PARENT-HANDOVER-2026-09-12.md](PARENT-HANDOVER-2026-09-12.md) の先頭
- 公開の手順: [RELEASE-PROCEDURE-0.4.md](RELEASE-PROCEDURE-0.4.md)（候補は版を据え置き、合格後に版を上げる）
- 合否の一式（固定）: 標準シナリオ 3 通り（MediaIn 5）＋ L-1 6 本 ＋ A 切替、すべて RTX
