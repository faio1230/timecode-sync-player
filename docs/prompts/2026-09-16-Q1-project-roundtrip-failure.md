# Q1: `SystemScenarioE2ETests.ProjectRoundTrip_RestoresPlaylistOrderOffsetModeAndPlayback` の既存失敗を切り分ける

作成: 2026-09-16 19:20、親。担当: 同期担当（`w5:p3`、作業ツリー `timecode-sync-player-wt-a`、ブランチ `agent-a`）。
基点: **main の最新**（T2 統合後）を `agent-a` へ通常マージしてから始める。

## 1. 事実

- R1（2026-09-16）の E2E 全件で、**main（mpv + CPU 合成）でも出荷構成（GStreamer + GPU）でも同じテストが 1 件だけ失敗**する。
  構成に依存しない既存の失敗で、原因は未調査。D8 統合後の親の E2E 全件でも失敗（実行 56・合格 55・失敗 1）
- E2E 全件が「既存失敗を除いて合格」という言い方を続けるのは v0.4 の完了判定に不都合。**直す前に、何が失敗しているかを確定する**

## 2. 依頼: 測って切り分ける（直すのはその後）

1. 単独で 3 回実行し、再現率と失敗の箇所（アサーション、期待値と実際の値、失敗までの時間）を記録する
2. 失敗が UIA 側（要素が見つからない、タイムアウト）か、アプリ側（プロジェクトの復元が実際に違う）かを切り分ける。
   アプリ側なら、`.tsp` の保存内容（プレイリスト順・オフセット・モード・再生状態）と復元後の状態を並べる
3. テスト自体の前提（素材のパス、`E2ESettingsIsolation`、起動時の既定構成）が変わったことで壊れたのか、製品の欠陥かを判定する材料を出す
4. `git log -S` 等で、このテストが最後に合格していた時期の見当を付ける（分かる範囲で）

## 3. 報告に入れるもの

再現率、失敗のメッセージ全文、切り分けの結論（UIA／テスト前提／製品欠陥のどれか、根拠）、直し方の案（実装はしない。親が承認してから）。**合否は書かない。**

## 4. 守ること

- 実機（E2E の起動）を使う前に一報。除去担当が段 2 の移設でビルドと E2E 2 件を回すので、順番は親が決める
- **公開リポジトリ**: 文書・コミットにローカルの絶対パスを書かない（`C:\Users\<user>`、`<repo>` のプレースホルダー）
- main への書き込みはしない。コミットは `agent-a`、日本語
