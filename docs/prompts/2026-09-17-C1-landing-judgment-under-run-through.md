# C-1 のテスト修正: ランスルーで保持中も進行する動画の着地判定

作成: 2026-09-17 18:40、親。担当: 除去担当（`w5:p6`、作業ツリー `timecode-sync-player-wt-b`、ブランチ `agent-b`）。
基点: **main の最新（`ce34740` 以降）** を `agent-b` へ通常マージしてから。
**テストコードの変更のみ。製品コードには触らない。判定を緩めるのではなく、ランスルーの仕様（保持中は動画が走り続ける）に合わせて期待位置を時間で動かす。**

## 1. 観測（自分の解析、C-1 の c1-01、2026-09-17 17:58）

- 17:58:53.416 `SeekTo target=3.231 ok=true`（timeline 8.0 − A.Start 5 = 3.0 に、サンプル時計の age 0.231s を足した目標）。着地は成立。
- 以降 LTC は 8.000 の保持。信号損失モードは既定のランスルーなので動画は走り続け、位置 3.231 → 6.300。
- テストの landed は |position − 3.000| ≤ 0.3 を 50ms 間隔で見るため、窓に居るのは約 70ms しかなく観測が外れた。D30 の確認 1 フレーム分だけ着地が後ろにずれ、さらに狭い。

## 2. 直すこと

1. `LtcScenarioE2ETests` の `CheckHold`（C-1 / C-2 / S-2 ほか保持着地の共通判定）で、着地の期待を **「着地目標 + 保持開始からの経過秒（ランスルーのとき）」** にする。信号損失モードが停止（`SetSignalLossMode(stop: true)` のシナリオ）は今のまま固定目標。既定（コンボ index 0）はランスルー。
   - 保持開始時刻は `signal.PlayHeld` を呼んだ時刻（ハーネス側）。期待 = target + (sampleTime − holdStart)、許容は今の 0.3 に加えて確認フレーム 1 枚分（1/fps）。
   - landed は窓に入った最初のサンプルで確定し、以後は「期待どおり進行しているか」を別の観測（`follow`）として残す（失敗条件にはしない。観測値だけジャーナルへ）。
2. ジャーナルに `expected`（時間で動く期待）と `observed` を両方残し、失敗時にどちらがずれたか分かるようにする。
3. 判定関数は `Helpers/` の共通関数にし、単体テストを 1 本（ランスルー: 着地 + 2.0s 後の期待が target+2.0；停止: 期待が動かない）。

## 3. 検証

- 非E2E 全件。
- E2E は **同期担当の S-2/C-1 実機（D31）と重ねない**。親の合図後に C-1 / C-2 / S-2 を 2 回ずつ。

## 4. 報告

コミット、変更の要約、単体の増減、E2E の結果、証跡パス。**合否は書かない。素材名・絶対パスを書かない。**
