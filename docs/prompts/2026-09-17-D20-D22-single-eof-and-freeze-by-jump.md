# D20〜D22: Single モードの終端と、ジャンプで入る Freeze 領域の表示（利用者の検証項目に対する欠陥）

作成: 2026-09-17 11:20、親。担当: 同期担当（`w5:p3`、作業ツリー `timecode-sync-player-wt-a`、ブランチ `agent-a`）。
基点: **main の最新（シナリオ E2E 統合後）** を `agent-a` へ通常マージし、shim を自分のツリーでビルドしてから。
仕様: 利用者の検証項目（`docs/LTC-SYNC-VERIFICATION-MATRIX-2026-09-17.md` 1 節の 7・9・21・22・24）。テスト: `tests/…/E2E/LtcScenarioE2ETests.cs` の S-2/S-3/S-4/S-5/F-3/F-4/F-5（開発機で失敗、`artifacts/ltc-scenarios/<testId>-<timestamp>` のジャーナルと PNG が観測）。
**製品コードの変更。同期の目標・補正（Smooth/Jump、サンプル時計）には触らない。ギャップ表示とトラック遷移の判断だけ。**

## 1. 欠陥

| # | 観測（開発機、2026-09-17 08:48〜08:51） | 仕様（利用者） | 直す場所の当たり |
| --- | --- | --- | --- |
| **D20** | Single + 同期 ON で、LTC がアクティブトラックの尺以上になると、終端へシークしたあと**次トラックへ自動前進**し、次も尺を超えるので連鎖して最後のトラックまで進む（S-2: LTC 20 で A → B → C、S-3/S-4/S-5 も同様。アプリログにシーク成功直後の次トラック Load） | 項目 7「Single で範囲外の LTC → その動画の終端で止まる」、項目 9「Single ではアクティブ以外は再生されない」。**Single + 同期 ON では終端到達で自動前進しない**（終端の最終フレームで静止。LTC が範囲に戻れば追従再開） | 終端到達 → 次トラックの自動前進（`PlaybackNearTrackEnd_AdvancesToNextTrack` の経路、`ContinueOnTrackEffects` / Single の EOF 処理、D11 の `single-eof-recovery` と整合させる） |
| **D21** | Continue + Freeze で、**ジャンプ**で Freeze 領域に入ると、前トラックの最終フレームにならない。F-4: B の中間（LTC 40）→ B の後のギャップ（52）で B 本文の絵（ジャンプ時点の表示）を保持。F-3: A の後のギャップ（27、A の最終フレーム保持中）→ B の後のギャップ（52）で**黒** | 項目 21「ジャンプで Freeze 領域 → 前の動画の最終フレーム」、項目 22「Freeze → 別の Freeze へジャンプで、その領域の前の動画の最終フレームに更新」。連続追従（F-1、合格）と同じ絵になること | ギャップ進入時の held 画像の決め方（`GapEnterCoordinator` / `GapFreezeHandler`）: 「今表示中の絵を保持」ではなく「**その領域の直前のトラックの最終フレーム**を読み込んでシークし、その絵を保持」。前トラックが未ロードでも読み込む（黒にしない） |
| **D22** | Continue + Freeze で、**最初のトラックの前**（LTC 1〜5 秒、先頭オフセット）が黒（F-5） | 項目 24「最初の動画の前のオフセット領域は最初の動画の冒頭フレーム（Freeze）」（Black のときは黒 = G-6 合格） | 先頭オフセット領域の Freeze: 直前のトラックが無い場合は**最初のトラックの冒頭フレーム**（MediaIn）を保持 |

## 2. 検証

- 単体: 該当コーディネータのテストに D20〜D22 の分岐を追加（既存テストの期待を変える場合はその理由を報告に書く）
- E2E: `LtcScenarioE2ETests` の 18 本すべて（実機は一報のうえ親の合図。除去担当と重ねない）。合格していた 11 本が崩れないこと。`RealProjectGapE2ETests`（V4 の代替プロジェクト、`TIMECODE_REAL_PROJECT_PATH`）と `LtcHardwareLoopE2ETests` も再実行
- V3 は不要（同期の補正に触れないため）。ただし `SingleModeSyncCoordinator` に触るなら `SyncSeekResync` 相当を 1 回

## 3. 報告

コミット（欠陥ごとに分ける）、変更の要約、単体の増減、E2E の結果（18 + V4 + 14）、証跡パス、設計差異。**合否は書かない。ローカルの絶対パス・素材名は書かない。バージョンは上げない。**
