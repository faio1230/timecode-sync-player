# v0.5.3 D38 の修正（保持後のジャンプの着地の遅れ）

作業ツリー: `C:\Users\<user>\Documents\timecode-sync-player-v053`（ブランチ `agent-a-v053-3a`）。git への書き込みは禁止（親がコミットする）。

根拠: `docs/design/v0.5.3-d38-seek-gates.md` §2〜§4、直す範囲は §6（親の判断）。

## やること

1. **赤いテストを先に書く**（`tests/TimecodeSyncPlayer.Tests/LatchLifetime/` か既存の SeekState のテストの近く）
   - (a) 保持の Duplicate の間に位置が着地の窓へ入ったら、pending が Settled になり位置の信頼が戻る
   - (b) 位置が未信頼の間に、pending の目標から 4×tol を超える新しい要求が来たら、pending を置き換える（タイムアウトを待たない）
   - 門 3: `JumpAppliedOnce` のために Jump を捨てたとき、ログが 1 行出る（振る舞いは変えない）
   - 現状で赤になることを確かめてから直す
2. **直す**（§6 のとおり。新しい門・新しい時間定数は足さない）
   - (a) 同期を適用しないフレームでも、pending があれば既存の着地判定を行い、Settled なら信頼を戻す。シークは出さない
   - (b) 未信頼の決定でも、要求の目標（クランプ済み）を置き換えの判定に渡す。置き換えた後の再確認とゲートは残す
   - 門 3: 捨てた箇所に `Information` のログ 1 行（例 `sync: Jump dropped (JumpAppliedOnce) ltc=… reason=…`）
3. **E2E の `latencyOverBudget` を失敗条件に切り替える**（`LtcScenarioE2ETests.CheckHold`）
4. **確かめる**（数字は報告に書く）
   - C-1 を 3 回: 19 ジャンプの `landingLatencySeconds` の最小・中央・最大（目安は二分探索の前の 0.05〜0.21 秒、上限 1.0 秒）
   - 修正の前（`6d5ab60` 相当）と後で、S-1〜S-4・C-1・C-2 の各 1 回について、1 周あたりの
     (i) 同期シークの回数、(ii) `pending "TimedOut"` の回数、(iii) 着地から 500ms 以内の同じ目標（±tol）への再シークの回数、
     (iv) 門 3 のログの回数 を表にする。(i)(iii) が増えたら報告で明記する
   - 非E2E（`--filter "FullyQualifiedName!~E2ETests" --logger trx`）全件、E2E（`--filter "Category=E2E&FullyQualifiedName!~LtcScenarioE2ETests"`）全件、
     LTC シナリオは `scripts/run-ltc-scenarios.ps1` の `-Filter 'FullyQualifiedName~LtcScenarioE2ETests&FullyQualifiedName!~L2_&FullyQualifiedName!~L3_'`（L-2・L-3 の長時間は回さない）
   - 既存のテストの期待値を変えた箇所があれば、1 つずつ理由を報告に書く
5. `docs/design/v0.5.3-d38-seek-gates.md` に §7「修正の結果」を足して、上の表と数字を書く

生ログ（`artifacts/ltc-scenarios/…`、アプリログ）は親のレビューが済むまで消さないこと。
