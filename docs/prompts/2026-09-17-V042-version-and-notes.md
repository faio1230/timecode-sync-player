# v0.4.2 のバージョン・CHANGELOG・リリースノート（統合は検証機の LTC 同期テストの後）

作成: 2026-09-17 09:00、親。担当: 同期担当（`w5:p3`、作業ツリー `timecode-sync-player-wt-a`、ブランチ `agent-a`）。
基点: **main の最新（`06fd035` 以降。D16・D16-b・D17-a 統合済み）** を `agent-a` へ通常マージしてから。**コードには触らない**（文書とバージョンだけ）。
前提: 検証機（ハイブリッド GPU、クリーン環境）で `0.4.1+1ea6dca` の AV1 6 本 + ProRes がクラッシュ 0・ロード 1.0〜2.2 秒・24fps 維持で通った（`docs/GSTREAMER-GPU-VALIDATION-PLAN-2026-09-12.md` 末尾）。利用者の方針「直ってから版を上げる」に従い、ここで 0.4.2 に上げる。**main への統合は検証機の LTC 同期 E2E の結果を見てから親が行う。**

## 1. やること

| # | 内容 |
| --- | --- |
| バージョン | csproj の `Version` を `0.4.2`（AssemblyVersion / FileVersion `0.4.2.0`）、`ApplicationVersionTests` のピンを `0.4.2` |
| CHANGELOG | `## 0.4.2 - 2026-09-17`（英語、CRLF、0.4.1 節と同じ体裁）。Fixed: D16（ハイブリッド GPU で、リングと別アダプタの d3d11 デコーダが AMD ドライバ内でクラッシュ。shim アダプタにデコーダ GUID が無い GPU プロファイルは skip して CPU プロファイルへ。v0.4.0 から存在）、D16-b（デコーダ要素をリングと同じアダプタのクラス `d3d11…device{N}dec` から選ぶ）、D17（不一致プロファイルごとに 3 秒待っていた。caps 不一致を検出したら即座に次へ。AV1 4K のロード 18.8 秒 → 約 1 秒）。Changed: 別アダプタのデコーダメモリを受け取った場合はクラッシュせずロード失敗として記録（`on_new_sample` の防御）。Known limitations: 0.4.1 のものに「内蔵 GPU が対応しないコーデック（AV1 など）は dGPU があっても CPU デコードになる（4K24 は実測で維持、4K60 は未確認）」を追加 |
| リリースノート | `docs/release-notes/v0.4.2.md`（日本語、0.4.1 と同じ構成）。冒頭に「v0.4.0 / v0.4.1 は内蔵 GPU + dGPU のノート PC で AV1 などの再生がクラッシュする欠陥があり、この版で修正。あわせてロード時間を短縮」。検証の概要は検証機の数字（AV1 6 本 + ProRes、クラッシュ 0、ロード 0.97〜2.2 秒 / 1.17 秒、frameUpdates 48〜49 / 2 秒）と開発機の回帰（V5 10/10、V3 平均 -30.9 / p95-p5 42.0、E2E 一部 6/0、非E2E 1674）。LTC 同期 E2E（検証機）は「（親が記入）」 |
| 計画文書 | `docs/release-0.4-plan.md` の「v0.4.1 の完了の定義」表の下に「v0.4.2 の完了の定義」: (1) 検証機で AV1 6 本 + ProRes がクラッシュ 0・ロード 3 秒未満（**合格** 08:05）、(2) 開発機の回帰（**合格**）、(3) 検証機の LTC 同期 E2E 14 件（空欄）、(4) 配布物の起動確認（空欄） |

## 2. 検証

- ビルド 0/0、非E2E 全件（`ApplicationVersionTests` を含む）、`git grep` でローカルの絶対パス 0、CHANGELOG が CRLF

## 3. 報告

コミット、変更ファイル、非E2E の件数。**合否は書かない。ローカルの絶対パスは書かない。** 実機は使わない。
