# 段階 7 の指示（文書化と引き継ぎ、コード変更なし）

worktree C:\Users\<user>\Documents\timecode-sync-player-wt-output-engine-20260911-1247（cwd 固定）。基点 a9b9349（段階 5＋R-1/P-1 は親確認で合格。全 E2E 55 成功・0 失敗）。
この段階は文書のみ。src/ と native/ のコードは変更しない（誤字修正も含めて変更しない）。

1. docs/ARCHITECTURE.md: GPU 出力経路（OutputBackend=Gpu）の構造を追記。OutputEngine（GPU worker）、ComposeLayer、IVideoSource（MpvSnapshotSource／GStreamerSource）、Spout worker（別デバイス＋共有フェンス）、vblank ゲート、lead 制御、TimelineOutputState、ExitCoordinator、GpuRecoveryState。スレッドモデルの図を更新。既存の CPU 経路の記述は残す。
2. docs/SETUP.md: GStreamer 1.28.2 MSVC x64 のインストール、GSTREAMER_1_0_ROOT_MSVC_X86_64、native/gst-shim のビルド手順（build-shim.ps1、vendor/Spout2 の取得 get-spout.ps1、tcs_gstreamer.dll の配置）、PlayerBackend／OutputBackend の設定項目、環境変数一覧（TIMECODE_SYNC_PLAYER_SETTINGS_PATH／SPOUT_NAME／OUTPUT_TRACE／TEST_CARD／SIMULATE_DEVICE_LOSS）。
3. docs/verification-checklist.md: GPU 経路の実機確認項目を追加（1080p/4K の 60/秒、Spout 60Hz、受信機断、キャンバス配置のスクリーンショット、テストカード、終了ダイアログ 3 経路、デバイス消失シミュレーション）。
4. native/gst-shim/README.md: 段階 6b 後の契約（別デバイス、共有リング 3 枚、共有フェンス、H-3 の取得規則、Ended）に合わせて全面を見直す。古い「Adopt」「FIFO(4)」の記述を削除。
5. docs/OUTPUT-GPU-DESIGN-CONFIRMED.md: 「段階 3 の設計差異」節を削除し、確定構造（Spout 別デバイス、デコーダ別デバイス＋リング）と、未解決事項（HAP、120Hz、複数画面、mpv×Gpu×Spout の実機、実デバイス消失）を更新。
6. 引き継ぎ: docs/HANDOVER-GPU-OUTPUT-2026-09-12.md（新規、最小）。ブランチと基点 SHA、検証 worktree、runner の使い方（Invoke-AppGpuTrial.ps1 の主要オプション）、集計スクリプト、既知の軽微事項、次の候補。

コミットは 1 つ（docs: …）。報告はコミット SHA と変更ファイル一覧のみでよい。
