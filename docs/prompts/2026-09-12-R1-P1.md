# 差し戻し R-1・P-1（段階 5 `3d5c4cf` の親評価、2026-09-12 04:00 JST）

worktree C:\Users\codea\Documents\timecode-sync-player-wt-output-engine-20260911-1247（cwd 固定）、基点 3d5c4cf。不変条件 docs/OUTPUT-GPU-INVARIANTS.md。

## 合格していること（親の実機・E2E）

- 終了ダイアログ E2E 3 件成功。runner からの通常終了（BtnExitNormal）で exit 0。
- デバイス消失シミュレーション: 1080p GStreamer（消失→復旧 90ms、位置連続、以後 60/秒）、2 回目の消失で Failed → BtnGpuRetry で復旧、mpv×Gpu も復旧。4K もリング再オープン成功。
- 非E2E 1712 件成功。

## R-1（必須）: 3d5c4cf で 4K の合成が毎秒 1 回 7〜12ms 停滞する（7b662c7 では出ない）

- 同条件（4K60 GStreamer×Gpu、Spout ON、DISPLAY2 全画面、消失シミュレーションなし）を連続で比較:
  - 3d5c4cf: `20260911T185700Z-app-4k-gst-s5-plain` 合成 p99 7.49ms、最大 9.47、毎秒 1 回 >3ms、lead 4.3→8.0ms、生成→走査 10.2ms
  - 7b662c7: `20260911T185832Z-app-4k-gst-s4build-plain-recheck` 合成 p99 0.85ms、>3ms は 30 秒で 3 回のみ
  - 消失シミュレーション run（`185104Z-app-4k-gst-s5-loss10`）でも 0〜8 秒と 19 秒以降に毎秒 1 回 10〜12ms。
- 段階 5 の変更のうち GPU worker の tick に入ったもの（MaybeSimulateDeviceLoss、ComposeLeadSuspension、ScanoutTracker.Reset、recovery.Phase の確認、recoveryRetry の待ち）と、UI 側の周期処理（ExitCoordinator／GpuRecoveryState の監視、1 秒タイマー、ロックの共有）を疑う。合成の内訳（acquire／フェンス待ち／描画）をトレースで切り分け、原因を特定して除くこと。4K plain 32 秒で合成 p99 1ms 以下・lead 3ms 以下に戻ったことを報告に含める（1080p では出ないので 4K で確認）。

## P-1（必須）: 既存 E2E 2 件が終了ダイアログで止まる

- `SettingsPersistenceE2ETests.SyncGapAndLtcDevice_ChangeExitAndRestart_RestoresSelections` と `SuccessfulProjectLoad_RecordsPathWithoutAutoOpeningOnRestart` が `Process.CloseMainWindow()` → `WaitForExit(5000)` で失敗（ダイアログが出て待ち続ける）。
- 修正: E2E ヘルパーに「通常終了（ダイアログの BtnExitNormal を押して exit を待つ）」を用意し、`CloseMainWindow()` 直後に exit を期待する既存テスト（7 箇所）をそれに置き換える。製品側の挙動（ダイアログ表示）は変えない。全 E2E（Category=E2E）を実行し、失敗 0 を報告（親の実行では 62 件中 失敗 2・スキップ 7）。

## 任意（L-4、今回はやらなくてよい）

lead が単発の 10ms スパイク 1 回で 8ms に跳び 20 秒戻らない（7b662c7 の 4K でも 19.8 秒に発生）。p99（60 標本では最大値）ではなく上位 2 番目を使う等は、親と相談してから。

1 コミット。報告は従来形式。R-1 は原因と証跡（どの変更が原因か）を明記すること。
