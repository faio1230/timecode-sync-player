# 段階 5 の指示（終了ダイアログとデバイス消失復旧）

worktree C:\Users\<user>\Documents\timecode-sync-player-wt-output-engine-20260911-1247（cwd 固定）。基点 7b662c7（段階 4 は親確認で合格: 1080p/4K の配置をスクリーンショットで確認、E2E 2 件成功）。
不変条件: C:\Users\<user>\Documents\timecode-sync-player\.superpowers\worktrees\session-refactor\docs\OUTPUT-GPU-INVARIANTS.md（I1〜I12。デバイス・スレッド・バッファ方針・ゲート規則・保存形式・終了順序の変更は実装せず質問する）。

仕様: C:\Users\<user>\Documents\timecode-sync-player\.superpowers\worktrees\session-refactor\docs\OUTPUT-GPU-STAGE4-5-SPEC.md の「段階 5」節（5.1〜5.4）をそのまま実装する。

- 5.1 `ExitCoordinator`: 遷移表のとおり。既定ボタンはキャンセル（Enter/Esc）。通常終了の手順は `MainWindowResourceDisposer` の順序（I8）を変えず、50ms 以上ブロックし得る手順は `Task.Run` で UI を止めない。強制終了は追加確認なしで `Environment.Exit(2)`＋2 秒の番人。全画面ウィンドウの ×／Esc は全画面解除のみ。
- 5.2 `GpuRecoveryState`: 自動復旧はプロセス寿命で 1 回、以後は `BtnGpuRetry` の手動のみ。復旧手順の順序は 5.2 のとおり（Spout 停止が先、ソース再接続が最後）。mpv 経路はリング再作成、GStreamer 経路は shim が別デバイスなので合成側だけ再作成し、リング・フェンスを開き直す（player は destroy しない。開き直せない場合のみ player 再生成＋位置シーク）。期限超過の fault は復旧に入れない（I9）。
- 試験フック `TIMECODE_SYNC_PLAYER_SIMULATE_DEVICE_LOSS=<秒>[,<秒>]`（起動時に 1 回読む。実デバイスは触らない）。
- 管理テストは 5.3、E2E は 5.4。

報告は従来形式（コミット、変更ファイル、非E2E 件数、実機の時刻と指標、設計差異、未検証）。実機は 1 プロセスずつ、自分の PID のみ、開始前に時刻を報告。仕様に無い判断が必要になったら質問する（選択肢＋根拠）。

## 同時に入れる小修正（段階 5 のコミットに含めてよい）

- L-3: lead 学習の除外を「世代変更（gst.generation／load／seek）・ソース接続・共有リング作成の後 1 秒」にも広げる。4K の再生開始直後に lead が 8ms へ跳ぶ（7b662c7 の 4K run、再生開始 5.8 秒→7.6 秒に 8.0ms）。管理テスト追加。

## 報告に含めること

コミット SHA、変更ファイル、非E2E 件数、実機（時刻・指標）、設計差異、未検証。段階 5 の実機は `TIMECODE_SYNC_PLAYER_SIMULATE_DEVICE_LOSS` で復旧経路を通し、終了ダイアログは E2E（5.4）で確認する。GPU リセット・ドライバー操作・OS 再起動はしない。

