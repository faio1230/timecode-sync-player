# v0.5.4 門の統合の前準備: 守っている欠陥の再発を押さえるテスト（担当 w5:p6）

作業ツリー: `timecode-sync-player-v054b`（ブランチ `agent-b-v054`、v0.5.4 に早送り済み）。絶対パスは `docs/local/LOCAL-PATHS.md`（公開しない）。
git への書き込みは禁止。**公開文書・コード・テストにローカルの絶対パスを書かない**（手元の pre-commit フックが止める）。**製品コードは変えない。**

根拠: `docs/design/v0.5.4-gate-unification.md` §2 の表の「再発を押さえるテスト（シナリオ層）」の列と §6 の段取り 2、
`docs/design/v0.5.3-d38-seek-gates.md` §3（門ごとに守っている欠陥）。

## やること

- §2 の表で「畳む」「消す」になっている門（3・5・7・9・10・11・12・14・17・20）それぞれについて、その門が守っている欠陥が
  **いまのコードで起きないこと**を、シナリオ層（`Integration/Scenario/`）のテストで固定する。すべて緑で入れる
  （統合のコミットで同じテストが緑のままであることが、その門を畳んでも欠陥が戻らない証拠になる）
- 既存のテストで既に押さえられている門は、そのテストの名前を表に書くだけでよい（新しく書かない）
- 14（デバウンス）は、連続したシークの要求が 250ms 以内に 2 本出ないことを、13 のゲートと 14 のそれぞれが止めている場面に分けて書く
  （14 を消した後に 13 だけで足りるかを見るため）
- `docs/design/v0.5.4-gate-unification.md` に §7「再発を押さえるテストの対応表」（門、守っている欠陥、テストの名前、新規／既存）を足す

## 確かめること

- 非E2E 全件（新しいテストの名前と件数を報告に書く）
- それぞれの新しいテストが、その門の条件を一時的に外すと赤になるか（確かめたものだけ報告に書く。コードは元に戻すこと）
