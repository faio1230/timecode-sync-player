# v0.5.4 D39 の修正（潰す一覧の K1・K2、シナリオ層の C5）（担当 w5:p6）

作業ツリー: `C:\Users\<user>\Documents\timecode-sync-player-v054b`（ブランチ `agent-b-v054`）。git への書き込みは禁止（親がコミットする）。

根拠: `docs/release-0.5-plan.md` の D39 節と「潰す一覧」の K1・K2、`docs/design/v0.5.4-scenario-layer.md` §4（再現の手順）と §7（赤いテストは修正と同じコミット）。

## やること

1. **赤いテストを先に書き、修正の前に赤になることを確かめて報告に書く**
   - K1（重い方）: ファイルを 2 本足す → 1 行目のオフセットを `00:00:10:00` に編集 → 長さの更新が後から届く → 編集したオフセットが 10 秒のまま（§4-2 の手順。ScenarioClock の予約で順序を固定）
   - K1 の続き: その状態でプロジェクトを保存 → 読み直すとオフセットが 10 秒で戻る（保存と読み込みの層で）
   - K2: 2 行目を選んだ状態で長さの更新が届いても、選択中の行（Id）が変わらない。行のインスタンスを差し替えるなら、差し替えの後に Id で選択を戻す、という不変条件を単体で固定する
2. **直す**
   - 長さの更新（`PlaylistState.UpdateMediaDuration`）と再計算（`RecalculateTimelineFrom`）が、利用者が編集したオフセットを上書きしないようにする。自動配置の再計算が、どの行のオフセットを動かしてよいかの規則を、コードとテストに書く
   - 選択が外れないようにする（行を差し替えずに更新する、または差し替えの後に選択を Id で戻す。どちらにしたかと理由を報告に書く）
   - 試験側の待ちを足して隠さない
3. **確かめる**
   - 非E2E 全件、E2E（`--filter "Category=E2E&FullyQualifiedName!~LtcScenarioE2ETests"`）全件
   - `SystemScenarioE2ETests.ProjectRoundTrip_RestoresPlaylistOrderOffsetModeAndPlayback` を単独で 10 回（v0.5.2 は 4/10 失敗）。失敗の回数と型を報告に書く
   - 既存のテストの期待値を変えた箇所があれば、1 つずつ理由を書く（変えずに済むのが望ましい）
