# v0.5.4: 決定的なシナリオ層の設計書（担当 w5:p6）

作業ツリー: `C:\Users\<user>\Documents\timecode-sync-player-v054b`（ブランチ `agent-b-v054`、v0.5.4 から切った）。
OpenCode はこのフォルダで起動し直すこと。git への書き込みは禁止（親がコミットする）。**この依頼は設計書だけ。製品コードもテストのコードも書かない。**

## 背景

- v0.5.4 は安定版（`docs/GOAL-v0.6.md` の 0 節）。シーク経路の 24 門（`docs/design/v0.5.3-d38-seek-gates.md` §3）を畳み、既知のエラー（D39 ほか）を潰す
- 門の統合の赤いテストは、実機の LTC シナリオ（音声のループバック・実時間・UIA）ではなく、プロセス内で決定的に回る層で書きたい。
  実機の E2E は 1 通り 8 分かかり、揺れ（UIA 100ms の読み、LTC の途切れ）で 1 回の結果を証拠にできない
- 既存の土台: `tests/TimecodeSyncPlayer.Tests/Integration/SyncScenarioHarness.cs`（本番の LTC 制御を UI なしで動かし、再生の操作と表示の境界だけを記録する）。
  D38 の再現テスト（`HeldLandingAfterJumpReproTests.cs`、`D38SeekGatesTests.cs`）もこれを使っている

## 書くもの: `docs/design/v0.5.4-scenario-layer.md`

1. **いまの土台の棚卸し**: `SyncScenarioHarness` が本物で動かしているもの・偽物に置き換えているもの・扱えないもの（時計、再生位置の進み、シークの着地の遅れ、ロードの完了、ギャップ、境界ホールド、信号断のタイマー、速度補正）
2. **足すもの**の設計
   - 仮想時計（`TimeProvider` と QPC を 1 つの時計から進める）
   - 偽の再生 API（シークの着地の遅れ・通り過ぎ・ロードの時間・長さの到着の遅れを設定で与えられる。`IPlaybackApi` の形に合わせる）
   - LTC の入力の台本（Normal／Duplicate／Jump／無音を時刻つきで並べる）
   - 観測（シーク・着地・一時停止・門の発火を時刻つきで取り出す。実機の E2E の hold-landing・jump-black・hold-pause と同じ定義の指標を出せること）
3. **実機の LTC シナリオとの対応表**: S-1〜S-5、C-1・C-2、R-1〜R-4、F-1〜F-5、G-1〜G-6 のうち、この層で同じ判定を再現できるもの・できないもの（理由つき）
4. **D39 の再現**を例として 1 つ書く（プレイリストにファイルを 2 本足す → 2 行目を選ぶ／1 行目のオフセットを編集する → 長さの更新が後から届く → 選択とオフセットがどうなるか）。
   D39 はプレイリストの層なので、同期の層と同じ時計で動かせるかも書く
5. **段取り**: 実装を何コミットに分けるか。各コミットで何のテストが緑になるか。既存のテストを壊さない順番
6. **利用者に諮ること**（あれば）

長さの目安は 200 行前後。根拠にしたファイルと行番号を書くこと。
