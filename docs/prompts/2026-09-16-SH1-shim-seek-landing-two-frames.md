# SH1: shim 実素材テストの「シーク後の最初のリースが目標から 2 フレーム後ろ」を切り分ける

作成: 2026-09-16 23:15、親。担当: 同期担当（`w5:p3`、作業ツリー `timecode-sync-player-wt-a`、ブランチ `agent-a`）。
基点: **main の最新** を `agent-a` へ通常マージしてから始める。**shim のコードは変えない（読む・測るだけ）。** 直すのは除去担当の段 3 統合後に別途。

## 1. 事実（除去担当の段 3 報告、2026-09-16 23:00）

- `native/gst-shim/test/shim_test.cpp` の実素材テスト（`test_1080p60.mp4`）で `failures=1`:
  `lease pts within one frame of the seek target`（post-seek pts=15.033 / target=15.000 / delta=33.3ms / frame=16.7ms）
- **段 3 前の main のビルドでも同じ結果** → 段 3 由来ではない既存の件。いつからかは不明
- 関連しそうな積み残し（引き継ぎ 7 節）: 「gst は load 直後に切替前のフレームを 2 回公開してから target へ飛ぶ」

## 2. 依頼: 測って切り分ける（直さない）

1. テストの期待（`within one frame`）と、シークの実装（`tcs_player_seek` → `gst_element_seek` のフラグ、`FLUSH` / `ACCURATE` / `KEY_UNIT`、
   シーク後の最初のサンプルの扱い、配信規則 H-3 の evict）を読み、**15.033 になる経路の候補**を列挙する
2. 素材の条件を確かめる: `test_1080p60.mp4` のキーフレーム間隔（`ffprobe -show_frames` で 15.000 前後の I フレーム位置）。
   15.000 がキーフレームか、直前のキーフレームがどこか
3. shim テストを **1 回だけ**実機で再実行し（`native/gst-shim/build-debug/tcs-shim-test.exe`。**親の合図の後、除去担当の実機が終わってから**）、
   stderr の `load.attempt` / `gst.delivery` / seek 前後の pts 列を回収する。シーク直後に配信された pts を順に並べる（15.000 / 15.017 が来て捨てられているのか、最初から 15.033 なのか）
4. いつからかの見当: `git log -S` で shim のシーク周りと配信規則の変更（H-3、D5、D8 の epoch）の日付を並べる
5. 直し方の案（実装しない）: テスト側の期待が厳しすぎる（2 フレーム以内が正しい）のか、shim がシーク直後の 2 フレームを捨てているのか、選択肢＋根拠で

## 3. 報告に入れるもの

候補の一覧、素材のキーフレーム位置、pts 列、いつからかの見当、直し方の案。**合否は書かない。**

## 4. 守ること

- shim・`Output/`・`RenderSession` は変えない（除去担当が段 3 を統合中）
- 実機（shim テストの実行）は一報のうえ合図を待つ
- **公開リポジトリ**: ローカルの絶対パスを書かない
- main への書き込みはしない。成果は報告（必要なら `docs/prompts/` へ調査メモとしてコミット、`agent-a`、日本語）
