# v0.5.4 K7（U-1）: 原因を測る（直さない）（担当 w5:p3）

作業ツリー: `C:\Users\<user>\Documents\timecode-sync-player-v053`（ブランチ `agent-a-v054`、v0.5.4 に早送り済み）。git への書き込みは禁止。

根拠: `docs/release-0.5-plan.md` の「潰す一覧」K7（利用者の決定 2026-09-26: 既知の制限に残さず v0.5.4 で直す。直し方を決める前に原因を測る）。

## 症状

U-1（`LtcScenarioE2ETests.U1_Continue_HighRateUiaAudit_KeepsSyncAndOutput`）: UI を 50ms ごとに UIA で読むと、配信が約 5 秒止まる区間がある
（v0.5.1〜v0.5.3 の全回。例: 「Held≥250ms 3 件（うち配信あり 0、最長 5150ms）」）。U-1 は実素材のプロジェクト（ギャップ無しの Continue）でだけ回る。
開発機では重い素材の周回（`-MediaDir C:\Users\<user>\Documents\timecode-sync-player-v05\artifacts\media-heavy -Media M1,M3,M4 -MediaInOffsetSeconds 5`）で回っている。

## 測るもの（直し方は決めない、製品の振る舞いは変えない）

1. **UIA の読み 1 回あたりの UI スレッドの占有時間**: どの UIA のプロパティ取得が UI スレッドへ渡り、何 ms 占有するか（自動化ピアの GetPattern／値の取得の内訳）
2. **配信が止まった区間の UI スレッドの処理内容**: 止まった区間（Held≥250ms）の間に UI スレッドで何が走っていたか。フレーム通知（`RenderSession` 経由、`DispatcherPriority.Background`）が UI スレッドのキューで待たされているのか、別の理由か
3. **配信の経路が UI スレッドに依存しているか**: 合成（OutputEngine の GPU worker）とフレーム配信が、UI スレッドの詰まりで止まる結びつきはどこか（コードの行で）
4. **通常の操作で起きないことの確認**: UIA の読みの間隔を 50ms・200ms・1000ms・読まない、で変えたときの配信の止まり（回数と最長）

計測のために一時的なログや計測コードを足すのは構わないが、報告の後に親が判断するまでコミットしない前提で、差分を分けておくこと。

## 報告に書くもの

- `docs/design/v0.5.4-u1-cause.md`: 1〜4 の測定値、原因の結論（どの結びつきで止まるか、根拠の行とログ）、直し方の候補を 1〜3 個（それぞれ触る範囲・危険・見積もり）
- 合成とフレーム配信の経路に触らずに済む直し方があるか（TSP-Fable の見立て: 表示用の値をキャッシュして UIA の読みを軽くする）
- 生ログは消さない（`artifacts\analysis-data` に置く）
