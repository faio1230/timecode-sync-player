# v0.5.2 段 1 の独立レビュー（w5:p3 向け）

親が段 1 を実装した（作業ツリー `timecode-sync-player-v05`、ブランチ `v0.5.2`、**未コミット**）。書いた者とは別の目で、
「振る舞いを変えていない」ことを確かめてほしい。**コードは直さない。所見を返すだけ。**

## 読むもの

1. `docs/design/v0.5.2-sync-state.md` の §3（段 1）・§4・§9（段 1 の実装の記録）
2. 差分: `git diff` と未追跡の 2 ファイル（`src/TimecodeSyncPlayer/SyncLifecycleEvent.cs`、
   `tests/TimecodeSyncPlayer.Tests/LatchLifetime/SyncLifecycleLogTests.cs`）。基準は HEAD（`0c3b3e9`）
3. 段 0 の表: `tests/TimecodeSyncPlayer.Tests/LatchLifetime/LatchLifetimeTable.cs`（「現状」の列が配線の入力）

## 確かめること

1. 入口ごとに、段 1 の前と後で「消すもの・呼ぶ順番・条件」が同じか。とくに次の 4 つ:
   - `MonitoringChanged` と `MonitorStopped`（検出中の条件、`MarkStopped` の後だけのポリシーの初期化、`SetMonitoring(false)` での再入）
   - `SyncEnabledChanged`（無効化でのシーク状態の破棄と追従開始の順番）
   - `TimecodeSyncService.BeginFileLoad`（処理を `OnLifecycle(FileLoad)` へ移した後の順番）
   - MainWindow のタイムラインのクリック（`CancelPendingSync` ＋ `ClearSeekState` → `TimelineSeek`）
2. 順番が入れ替わった所があれば、入れ替わった処理どうしが互いの状態を読まないか
3. `GapFreezeHandler.SetState` を全部の書き込みが通るか（ログだけで、状態機械の振る舞いが変わらないか。別スレッドからの書き込みの有無）
4. `BeginFileLoad` を通らない読み込み（GPU 復旧・自動送り・ギャップの LoadPausedAt）から `FileLoad` が出ていないこと
5. ログの文言 `Sync lifecycle: {Event} source={Source}` が、既存の E2E・解析スクリプトの読むログの行と衝突しないか（`scripts/` と E2E を grep）
6. フレーム経路に残した消し方の一覧（§9）に抜けがないか

## 規則

- **git の書き込み禁止**（add / commit / stash / reset / checkout / clean）。作業ツリーは親と共有している
- テストは非 E2E だけ回してよい（`--filter "FullyQualifiedName!~E2ETests"`）。**E2E と実機は回さない**（親が回している）
- 返すもの: 確かめたこと 1〜6 それぞれの結果（問題なし／問題あり＋ファイル:行と理由）。
  「振る舞いが変わる」の指摘は、変わる入力と結果を具体的に書く
